---
- name: Playbook to install Nginx on Webservers
  hosts: pub
  become: yes
  tasks:
    - name: Perform Apt update and Install basic packages
      ansible.builtin.shell: apt update && apt install -y unzip jq net-tools
